# Firebase Studio

Este proyecto requiere la variable de entorno `NETLIFY_BLOBS_TOKEN` para guardar y cargar datos usando Netlify Blobs.

**Configura esta variable en tu panel de Netlify en Site settings → Environment variables.**

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.
